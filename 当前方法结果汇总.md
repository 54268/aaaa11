# 当前方法结果汇总

本文件汇总当前主方法在 WiSig 与 Oracle 两个数据集上的开放集拒识和未知类细分结果，便于直接查看。数值来自当前正式输出目录中的 JSON 指标文件。

## 开放集拒识指标

| 数据集 | 总体准确率 | 已知类准确率 | 未知类精确率 | 未知类召回率 | 宏平均 F1 | AUROC | FPR95 | OSCR | 阈值模式 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| WiSig | 0.997204 | 0.986719 | 0.996471 | 1.000000 | 0.993554 | 0.995952 | 0.010937 | 0.986684 | `manual_global` |
| Oracle | 0.966187 | 0.963625 | 0.992813 | 0.967042 | 0.936905 | 0.986836 | 0.035375 | 0.957482 | `manual_classwise` |

## 未知类细分指标

| 数据集 | NMI | ARI | Purity | 匈牙利准确率 | 目标未知类数 | 候选分量数 | 最终有效簇数 | 覆盖率 | 不确定样本数 | 细分特征 | 细分后端 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- | --- |
| WiSig | 0.998125 | 0.998637 | 0.999375 | 0.999375 | 12 | 12 | 12 | 1.000000 | 0 | `embedding_stats` | `gmm_full_direct` |
| Oracle | 0.999112 | 0.999469 | 0.999801 | 0.999801 | 6 | 8 | 6 | 0.839458 | 3147 | `embedding_stats` | `gmm_full_direct` |

## 实验协议摘要

### WiSig

- 输出目录：`D:\learn_pytorch\笔记\方案\os_sei_code\outputs\wisig_singleday_osr_k16_u12`
- 已知类数量：`16`，未知类数量：`12`
- 训练 / 验证 / 已知测试 / 未知测试样本数：`8960` / `1280` / `2560` / `9600`
- 拒识策略：`manual_global`；分数校准：`none`；已知类救回：`False`
- 细分方法：`unknown_only_gmm_full_direct`；是否启用已知原型锚点：`False`
- 细分候选分量：`12`，即 `K=12, m=0`；最终有效簇：`12`；不确定过滤比例：`0.000000`
- 说明：WiSig 已同步为 `embedding_stats + PCA96 + unknown-only GMM-full-direct`；冗余候选因子在 `m ∈ {0, 1, 2}` 中固定主结果，当前 WiSig 取 `m=0`，避免把干净未知类拆成类内子模态。

### Oracle

- 输出目录：`D:\learn_pytorch\笔记\方案\os_sei_code\outputs\oracle_kri16_demod_known_first`
- 已知类数量：`10`，未知类数量：`6`
- 训练 / 验证 / 已知测试 / 未知测试样本数：`28000` / `4000` / `8000` / `24000`
- 拒识策略：`manual_classwise`；分数校准：`classwise_z`；已知类救回：`True`
- 细分方法：`unknown_only_gmm_full_direct`；是否启用已知原型锚点：`False`
- 细分候选分量：`8`，即 `K=6, m=2`；最终有效簇：`6`；不确定过滤比例：`0.134619`

## 运行入口说明

- `run_wisig.py` 和 `run_oracle.py` 中 `RUN_UNKNOWN_SUBDIVISION = True` 时，运行主入口会在开放集拒识结束后自动继续执行未知类细分。
- 因此平时完整刷新结果只需要运行对应主入口，不需要再单独运行 `run_wisig_subdivision.py` 或 `run_oracle_subdivision.py`。
- 两个 subdivision 入口主要用于复用已有闭集模型和拒识配置，只刷新拒识中间结果或细分报告。

## 指标含义简表

- `known_accuracy`：真实已知类样本被正确识别为对应已知类的比例。
- `unknown_precision`：被判为 unknown 的样本中真实未知类样本的比例。
- `unknown_recall`：真实未知类样本被拒识为 unknown 的比例。
- `fpr95`：未知召回约 95% 时，已知样本被误拒为 unknown 的比例，越低越好。
- `NMI` / `ARI` / `Purity` / `hungarian_accuracy`：用于评估未知类细分聚类与真实未知类标签的一致性。
- `coverage_of_total_test_unknown`：真实未知测试样本中最终参与细分评估的比例。
