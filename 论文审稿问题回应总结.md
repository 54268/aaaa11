# 论文审稿问题回应总结

本文档根据当前论文 PDF、LaTeX 源文件 `main.tex` 和项目内最新实验结果整理，用于后续回复审稿意见和修改论文。核心原则是：能用实验解释的地方给出清楚口径；确实存在表述不完整或协议假设过强的地方，直接承认并在论文中补充。

## 0. 最新结果口径

当前最终主表采用 OSCR，不再用 AUROC 作为开放集主比较指标。

### 开放集拒识

| Dataset | Known Acc. | Unknown Rec. | Macro-F1 | OSCR |
| --- | ---: | ---: | ---: | ---: |
| Oracle | 94.55 | 96.52 | 92.81 | 94.15 |
| WiSig | 97.07 | 100.00 | 98.57 | 97.06 |

### Oracle 未知类细分

| Method | NMI | ARI | Hungarian Acc. | Coverage |
| --- | ---: | ---: | ---: | ---: |
| K-means | 85.24 | 83.22 | 92.18 | 96.71 |
| HDBSCAN | 68.48 | 42.59 | 48.75 | 69.92 |
| OpenRFI | 93.19 | 90.99 | 93.02 | 85.00 |
| PCBM (ours, auto K) | 96.34 | 95.48 | 97.93 | 94.31 |

### WiSig 未知类细分

| Method | NMI | ARI | Hungarian Acc. | Coverage |
| --- | ---: | ---: | ---: | ---: |
| K-means | 91.71 | 80.65 | 81.51 | 100.00 |
| HDBSCAN | 96.32 | 93.34 | 92.26 | 97.59 |
| OpenRFI | 98.11 | 96.76 | 96.23 | 92.45 |
| PCBM (ours, auto K) | 99.81 | 99.86 | 99.94 | 100.00 |

当前开放度曲线和多开放度表如需入论文，应以 supervised calibrator + true auto-K 的当前 final 结果重新导出；历史开放度表不再作为当前主结果口径。

## 1. 创新性不足：PCBM 与普通边界样本/已有伪未知方法的差异

**问题概括：** 审稿人可能认为 PCBM 只是“找边界样本 + 生成伪未知 + OpenMax/距离融合”，与已有边界样本或伪未知方法差异不够突出。

**回应判断：** 这个意见需要认真回应。当前论文已经写了 local marginality 和 prototype competition，但没有把“竞争关系为什么不可替代”讲透。

**建议回答：** PCBM 的核心不是单纯选择远离类中心的样本，而是在 marginal boundary samples (MBS) 上进一步建模相邻已知原型之间的竞争关系。普通边界样本只描述“离本类原型远不远”，PCBS 还描述“是否同时靠近强竞争原型”。这类样本更接近 known-known 决策冲突区，也更可能对应 unknown-to-known 混淆风险。

**论文应补充：**

- 在方法部分明确区分 MBS 和 PCBS：MBS 是径向/局部稀疏边界，PCBS 是带相邻原型竞争的高风险边界。
- 在消融分析中强调 PCBM 的贡献应分成拒识校准和未知类细分两部分说明。当前主表的拒识结果为 Oracle 94.55/96.52/92.81/94.15，WiSig 97.07/100.00/98.57/97.06；未知类细分结果为 Oracle 96.34/95.48/97.93/94.31，WiSig 99.81/99.86/99.94/100.00。
- 如果篇幅允许，增加一个 PCBS vs ordinary MBS 的可视化或分数分布，证明竞争边界样本更靠近真实未知混淆区。

## 2. 数据集协议和可复现性不足

**问题概括：** 当前论文只说使用 ORACLE 和 WiSig，并说明训练只用已知类、测试引入未知类，但没有充分列出具体 emitter split、样本数、接收端/日期设置和调参协议。

**回应判断：** 这个意见成立，建议在实验设置中补表。

**建议回答：** 所有方法均采用固定 known/unknown split。训练、验证和阈值选择只使用 known training/validation 及由 known 边界外推得到的 pseudo-unknown，真实 unknown 仅用于最终测试。

**当前可写入论文的协议表：**

| Dataset | Known classes | Unknown classes | Train | Val | Known test | Unknown test |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Oracle | 10 | 6 | 28000 | 4000 | 8000 | 24000 |
| WiSig | 16 | 12 | 8960 | 1280 | 2560 | 9600 |

Oracle known emitter IDs: `1, 2, 3, 4, 5, 7, 9, 13, 14, 15`.

Oracle unknown emitter IDs: `17, 18, 19, 25, 26, 32`.

WiSig known IDs: `13-3, 20-7, 7-10, 6-15, 3-13, 11-4, 8-3, 7-11, 15-1, 3-18, 4-11, 14-7, 11-7, 5-5, 11-1, 1-11`.

WiSig unknown IDs: `20-19, 6-1, 2-19, 16-16, 20-15, 8-20, 10-7, 11-17, 8-18, 10-11, 20-12, 14-10`.

**需要谨慎表述：** WiSig 当前是 single-day/fixed-RX 协议，不能把 100% unknown recall 解释成跨日期、跨接收端、跨域泛化都已经解决。论文里可以说“在当前固定接收端协议下”，不要过度外推。

## 3. 基线公平性和阈值选择

**问题概括：** 审稿人可能质疑 Softmax、OpenMax、ARPL、HyDRA、OpenRFI、HyperRSI 是否使用了同等协议，阈值是否借用了测试未知类。

**回应判断：** 需要补充统一协议说明。当前论文“same known/unknown split protocol”说得太简略。

**建议回答：** 所有开放集方法使用同一 known/unknown split；训练阶段不使用真实 unknown 测试样本。PCBM 的阈值和校准参数来自 known validation、OpenMax EVT、prototype distance statistics 和 feature-level pseudo-unknown，不使用真实 unknown test labels。比较方法也应按其原方法要求在相同训练/验证划分下调参，并在同一测试集上评估。

**论文应补充：**

- 增加 baseline implementation protocol 表，列出输入、backbone/特征、阈值选择数据、是否需要额外生成样本。
- 明确最终开放集主指标是 Known Acc., Unknown Recall, Macro-F1, OSCR。
- AUROC 只作为 unknown score separability 的辅助图或附录指标，不再作为主比较表列。

## 4. 方法公式和关键实现细节不完整

**问题概括：** LaTeX 中总损失只写了 `L = Lce + lambda_ang Lang + lambda_pro Lpro`，但没有给出三个子损失的明确定义。校准器、OpenMax tail size、阈值、GMM/PCA 等细节也需要更完整。

**回应判断：** 这个意见成立，建议在方法或附录中补公式和超参数表。

**建议回答：** 修订版将补充分类损失、角度间隔损失和原型紧致损失的完整定义，并给出关键实现参数，包括 embedding dimension、OpenMax tail size、prototype-distance boundary、calibration MLP、PCA dimension、GMM covariance type、auto-K candidate range 和 GMM fit-filter-merge criterion。

**必须修正的稿件矛盾：**

- 摘要和 Fig. 2 当前有“deep embeddings + distance relationships to known prototypes”的表述。
- 代码和正文 Eq. (25) 的实际细分输入是 `h_i = [z_i; s_i]`，也就是 128 维 embedding + 49 维 I/Q statistics，并不是 `[distance vector; statistics]`。

**建议处理：** 如果不打算改代码，就把摘要、框架图和相关文字统一改成“deep embeddings and I/Q statistical descriptors”。不要继续说最终 GMM 输入包含 known-prototype distance vector。

## 5. 特征层伪未知的物理合理性

**问题概括：** 审稿人可能质疑 feature-level pseudo-unknown extrapolation 是否对应真实可观测的 I/Q 波形，是否有物理合理性。

**回应判断：** 不能过度声称伪未知是“真实新发射源”。当前方法生成的是 feature-level risk surrogate。

**建议回答：** PCBM 不生成新的 I/Q waveform，也不声称 feature-level pseudo-unknown 是可直接物理发射的信号。它的作用是构造已知类边界外侧的 open-set risk samples，用于监督校准 unknown probability。也就是说，伪未知是开放集决策边界的训练代理，而不是真实 RF 波形生成器。

**论文应补充：**

- 在方法部分保留“PCBM does not synthesize new I/Q waveforms”的说明，并进一步强调它是 feature-level surrogate。
- 在局限性中承认物理可解释性仍有限。
- 可增加 pseudo-unknown、真实 unknown 和 known boundary 的 t-SNE/score distribution，用实验说明伪未知确实帮助校准边界。

## 6. 未知类细分是否假设已知未知类数量 K

**问题概括：** Algorithm 2 输入包含 target unknown number `K_u`，K-means 也需要 K。审稿人可能质疑这在真实开放环境中过于理想化。

**回应判断：** 这个质疑在历史给定 K 诊断口径下成立；当前主结果已切换为自动候选 K 搜索，不再把真实未知类数作为输入。

**建议回答：** 当前 PCBM 主结果不预先给定未知类数，而是在统一候选范围内自动搜索 GMM 候选分量数 `fit_K`，并通过 GMM 拟合后的候选筛选与簇均衡自动合并得到最终有效未知簇 `effective_K`。Oracle 自动得到 `fit_K=8, effective_K=6`，WiSig 自动得到 `fit_K=13, effective_K=12`。K-means 作为传统聚类基线仍需要给定评估协议中的 K，因此在论文中应明确它不是完全开放场景的自动发现方法；HDBSCAN 不需要预设 K，但会产生较多 noise；OpenRFI 使用其自身的原型图分组和置信筛选机制。

**论文应补充：**

- 在实验设置中明确：当前 subdivision 主实验不把 `K_u` 作为输入，而是在统一候选范围 `fit_K=2..20` 内自动选择 GMM 候选分量数。
- 在方法部分说明自动 K 评分由 GMM lower bound、簇规模均衡项和复杂度惩罚组成，真实未知标签只在离线评估中使用。
- 在 limitation 中说明候选范围仍是一个开放集模型选择超参数，未来可继续探索 Dirichlet-process GMM 或更强的非参数密度聚类。

## 7. 覆盖率 coverage 的定义和容易被质疑的地方

**问题概括：** 用户已指出：有的表中前三个消融变体 coverage 都是 96.52，因为直接继承当前 formal 拒识 unknown recall；但 HDBSCAN coverage 是 69.92，OpenRFI 是 85.00；这容易让审稿人质疑 coverage 的定义不一致。

**回应判断：** 需要在论文中把 coverage 定义写清楚，并明确不同方法丢样本发生在哪一步。

**统一定义：**

```text
Coverage = 最终被分配到有效未知簇的真实未知测试样本数 / 全部真实未知测试样本数
         = unknown-cache recall × selected-unknown valid-cluster coverage
```

**关键解释：**

- Embedding only 和 I/Q descriptors only 用于检验单一特征；Feature fusion 是当前 PCBM 未知类细分主流程，使用 embedding 与 I/Q 统计描述拼接后执行标准化、PCA 和 auto-K GMM。
- 当前表中只保留 `Embedding only`、`I/Q descriptors only` 和 `Feature fusion` 三行；auto-K 选择分析单独报告 Oracle 的 `fit_K=8, effective_K=6, coverage=94.31`，WiSig 的 `fit_K=13, effective_K=12, coverage=100.00`。
- HDBSCAN 的 coverage 低，是因为它在细分聚类阶段会把低密度样本标为 noise `-1`。
- OpenRFI 的 94.57 是开放集拒识表中的 unknown recall；85.00 是其细分协议下最终保留高置信分组的真实未知样本比例。两者不是同一个指标。
- OpenRFI 不是在 PCBM unknown cache 一开始做预筛选；当前适配流程是 full-test-world prototype grouping 后按置信度保留有效分组。
- K-means 不会输出 `-1`，所以在给定 K 后会把进入 unknown cache 的样本全部硬分到某个簇。它的 coverage 约等于其拒识缓存召回率。若表中出现 96.52 和 96.71 这类差异，通常意味着使用了不同 open-set prediction source；论文中同一张表应明确每个方法的 coverage 是在其自身细分协议下统计，或统一到同一个 prediction source。

**论文应补充：** 在 Table V 或指标定义段加一句：coverage is computed over all true unknown test samples after both rejection and subdivision filtering; methods that do not output noise or uncertain samples have coverage equal to their unknown-cache recall.

## 8. “拒识是 96%，细分上线变 89%”的问题

**问题概括：** 之前出现过使用旧目录或旧配置导致 Oracle 细分/上线口径变成 89 左右的问题，与当前 formal 拒识 96.52 不一致。

**回应判断：** 这是实验结果管理问题，不应进入论文。最终论文和所有主表必须统一使用最新完整 PCBM 结果。

**当前正确口径：**

- Oracle open-set unknown recall: 96.52。
- Oracle full subdivision coverage: 94.31。
- Oracle full subdivision resolved clusters: 6，不是 5。
- 旧的 `outputs/oracle_supervised_calibrator/final` 是监督校准单独实验目录，不作为最终主表来源。
- 当前最终主表来源是 `ablations/04_细分流程消融/oracle/full_subdivision` 和 formal final tables。

## 9. 统计显著性和鲁棒性

**问题概括：** 单次 split 的结果可能不足以证明统计稳定性；同时 WiSig 固定接收端协议不能代表跨域泛化。

**回应判断：** 需要补充或在局限性中承认。

**建议回答：** 当前实验已经包含不同 openness 的 OSCR 曲线，说明随着未知类增加，PCBM 仍保持稳定。但修订版应进一步增加多 seed 或多 split 的 mean ± std。如果计算资源有限，至少对主要表格中的 PCBM、OpenRFI、OpenMax、K-means 等关键方法做 3 次重复。

**论文应补充：**

- 多随机种子或多 known/unknown split 的均值和标准差。
- 若不做跨日期/跨接收端实验，则避免使用“跨域鲁棒”这类过强表述。
- 结论中可写“future work will study cross-receiver and cross-day generalization”。

## 10. 图表、文字和格式问题

**贡献表述：** 建议把三条贡献写得更具体：PCBS 边界挖掘、feature-level pseudo-unknown supervised dual calibration、post-rejection subdivision。

**图 8/9/10：** 字体、坐标轴、图例需要放大，避免 PDF 双栏下不可读。

**数据集命名：** 统一使用 ORACLE 和 WiSig。当前稿件里有 Oracle/ORACLE 混用。

**英文细节：** 例如 `environments.The` 中间缺空格，需要全文 proofread。

**参考文献：** arXiv、会议、期刊格式需要统一，尤其是年份、页码、DOI 和大小写。

**复杂度：** 即使本文不主打 lightweight，也建议补一个简短复杂度表：参数量、推理时间、训练额外开销、细分阶段 GMM/PCA 开销。这样可以回应部署可行性和复现问题。

## 11. 可以直接用于回复审稿人的简短版本

We thank the reviewer for pointing out the need for clearer protocol and implementation details. In the revised manuscript, we will explicitly distinguish marginal boundary samples from prototype-competitive boundary samples, add complete loss definitions and hyperparameter settings, and clarify that the pseudo-unknown samples are feature-level risk surrogates rather than synthesized I/Q waveforms. We will also provide the exact known/unknown splits, sample counts, threshold-selection protocol, and baseline adaptation details to make clear that no real unknown test samples are used during training or calibration.

For unknown-emitter subdivision, we will clarify that the current main experiment follows a controlled protocol where the target number of unknown subcategories `K_u` is given. K-means and PCBM-GMM use this protocol value, whereas HDBSCAN can output noise and OpenRFI applies confidence-based grouping in its adapted subdivision pipeline. Therefore, the reported coverage denotes the fraction of all true unknown test samples finally assigned to reliable unknown clusters after both rejection and subdivision filtering, not simply the open-set unknown recall. We will add this definition near the metric description and revise the subdivision discussion accordingly.

Finally, we will update all main comparison tables to report OSCR as the primary open-set trade-off metric, keep AUROC only as a diagnostic score-separability result, and add robustness or limitation discussions for multi-split stability and cross-receiver/cross-day generalization.
